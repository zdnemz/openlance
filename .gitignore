# dependencies
node_modules
/.pnp
.pnp.*

# testing
/coverage

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# logs
*.log
npm-debug.log*
yarn-debug.log*

# typescript
*.tsbuildinfo

# env files (opt-in via .env.example)
.env*
!.env.example

# local/platform dirs
/local-*
/prompt
.claude
.z-ai-config
/skills/
/upload/
/download/
.zscripts/
.foundry/

# agent session log (kept on disk, not part of the product)
worklog.md

# codegraph indexes (local tooling cache)
.codegraph/

# vercel
.vercel

# backend runtime state (live devnet data, regenerated per boot)
scripts/anvil/.state/
scripts/anvil/.anvil-deployment.json
data/

# drizzle-kit push state (regenerated by `db:migrate`, not migration history)
/drizzle/
